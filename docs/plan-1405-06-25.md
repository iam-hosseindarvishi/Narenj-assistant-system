# برنامه اجرایی — جلسه بعد (۲۵ شهریور ۱۴۰۵)

> وضعیت فعلی: داشبورد اصلی + پنل مغایرت‌یابی روی استک FastAPI/PostgreSQL/Redis/Vue پوش شده و سالم است (کامیت `987ddfc`). تسک‌های زیر به ترتیب اولویت آماده اجرا هستند.

## ۱. تنظیمات داشبورد اصلی (اصلاحات UI)

- [ ] **حذف ساعت از appbar بالای داشبورد اصلی** — ساعت و تاریخ فقط در bottom bar می‌ماند (الان در هر دو تکرار شده)
- [ ] **دکمه خروج در بالای داشبورد** (کنار عنوان، آیکون ⏻)
- [ ] **دکمه تغییر کاربر** — خروج + هدایت مستقیم به صفحه ورود (بدون intermediate)
- [ ] **دکمه تغییر تم لایت/دارک** — آیکون 🌙/☀️ در appbar
- [ ] **پیاده‌سازی تم دارک برای تمام صفحات** — پیشنهاد فنی:
  - Tailwind `darkMode: 'class'` در `frontend/tailwind.config.js`
  - استور کوچک `stores/theme.ts` با ذخیره در `localStorage` و toggle کلاس `dark` روی `<html>`
  - تبدیل کلاس‌های کامپوننت‌ها به الگوی `bg-white dark:bg-slate-800` و امثال آن
  - صفحات: MainDashboard، Login، AdminLayout + ۹ ویو مغایرت‌گیری
  - کلاس‌های مشترک (`card`, `input`, `table-base`, `btn-*`) در `style.css` به‌روزرسانی می‌شوند تا تغییرات حداقلی بماند

## ۲. مدیریت کاربران و دسترسی‌ها (پیش‌نیاز سطوح دسترسی)

- [ ] API بک‌اند: CRUD کاربران (فقط admin) — `POST/PUT/DELETE /api/users` + تغییر گذرواژه
- [ ] مدل: ستون `permissions` (JSON) روی `users` برای دسترسی per-user به ماژول‌ها — جایگزینی `permissions_for_role` در `core/permissions.py`
- [ ] صفحه «مدیریت کاربران» در پنل: لیست، افزودن، ویرایش نقش/دسترسی، ریست گذرواژه
- [ ] اتصال دکمه‌های داشبورد اصلی به همین دسترسی‌ها (الان فقط نقش‌محور است)

## ۳. تکمیل پنل مغایرت‌گیری

- [ ] **صفحه مدیریت قالب‌ها** — ویرایش mapping ستون‌ها برای اکسل‌های با ساختار متفاوت (API از قبل موجود است: `PUT /api/templates/{id}`)
- [ ] **تطبیق دستی لایه ۴** — انتخاب چندگانه POS + بانک + حسابداری در `ManualMatchingService.link` (از قبل پشتیبانی می‌شود، UI ندارد)
- [ ] رفع شمارنده لایه ۲ در داشبورد مغایرت‌گیری (الان `pending` با تعداد ثبت‌شده جایگزین می‌شود و با متن کارت نمی‌خواند)
- [ ] ثبت کارمزدها (`register`) از UI لایه ۲ تست شود

## ۴. زیرساخت و استقرار

- [ ] **CI با GitHub Actions** — اجرای pytest روی هر push + build دو ایمیج داکر
- [ ] **Alembic** — مهاجرت‌های واقعی به‌جای `create_all` در استارتاپ (برای تغییرات آینده اسکیمای `users.permissions`)
- [ ] اسکریپت استقرار سرور: تولید `JWT_SECRET`، نوشتن `.env`، `docker compose up -d`
- [ ] صفحه `/api/docs` را روی nginx فقط برای شبکه داخلی باز بگذار (الان عمومی است)

## ۵. بعد از آن (backlog)

- ماژول گزارش‌های عمومی در ردیف «گزارش‌ها» داشبورد اصلی
- نمودار روند مغایرت‌ها (chart.js یا مشابه) در داشبورد مغایرت‌گیری
- اعلان‌ها/تستم گزارش روزانه
- صفحه‌بندی جداول لایه ۴ (۱۱۲۰ رکورد در یک جدول رندر می‌شود)

---

**نکته اجرایی:** بعد از هر تغییر فرانت فقط `docker compose up -d --build frontend` لازم است؛ تغییرات بک‌اند با volume زنده اعمال و با `docker compose restart backend` لود می‌شوند. تست‌ها: `docker compose exec backend python -m pytest -q` (فعلاً ۴۲ پاس).
